# This repository does not define any Python application dependencies.
# It is a JavaScript/TypeScript project with:
# - backend/package.json
# - task-management-frontends/package.json
#
# So there are no repo-derived packages to install with:
#   python -m pip install -r requirements.txt
#
# Install the actual project dependencies with npm instead:
#   cd backend && npm install
#   cd task-management-frontends && npm install
